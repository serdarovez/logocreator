export type LogoCategory = "fashion" | "car" | "food" ;

export type LogoOptions = {
  name: string;
  category: LogoCategory;
  color: string;
  font: string;
  backgroundColor:string;
  textColor:string
};
